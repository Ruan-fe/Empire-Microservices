server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
security:
  jwt:
    token:
      secret-key: chave_microservices
      expire-length: 360000

